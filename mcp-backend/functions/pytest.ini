[pytest]
addopts = --import-mode=importlib
pythonpath = .
markers =
    integration: integration tests that hit real services
    email: tests that send real emails
filterwarnings =
    ignore::FutureWarning:google.api_core
    ignore::DeprecationWarning:importlib
